const { FieldType } = require('tablestore');

const {
  LONG,
  DOUBLE,
  BOOLEAN,
  KEYWORD,
  TEXT,
  NESTED,
  GEO_POINT,
} = FieldType;

module.exports = {
  LONG,
  DOUBLE,
  BOOLEAN,
  KEYWORD,
  TEXT,
  NESTED,
  GEO_POINT,
};
